apiVersion: ascender.ansible.com/v1beta1
kind: AscenderBackup
metadata:
  name: example-ascender-backup
spec:
  deployment_name: example-ascender
  backup_resource_requirements:
    limits:
      cpu: "1000m"
      memory: "4096Mi"
    requests:
      cpu: "25m"
      memory: "32Mi"
